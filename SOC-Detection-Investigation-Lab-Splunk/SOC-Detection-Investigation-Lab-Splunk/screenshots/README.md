# Screenshot Evidence

Replace the placeholder PNG files with real screenshots while keeping the exact filenames.

## Required

The ten files in `required/` complete the published project.

## Optional

The three files in `optional/` can be replaced when the related telemetry is available. The project does not claim these optional scenarios were completed unless the real screenshot is supplied.

## Evidence Handling

- Do not expose passwords or tokens.
- Mask public IP addresses and unrelated usernames.
- Keep relevant time, host, account, event ID and outcome fields visible.
- Do not edit event values or fabricate results.
